model Grid {
    id        String   @id @default(cuid())
    top       Int
    left      Int
    bottom    Int
    right     Int
    gridData  String   // JSON文字列として保存
    createdAt DateTime @default(now())
    // 必要に応じてユーザーとの関連付けも追加
} 